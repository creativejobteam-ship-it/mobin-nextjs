export interface AppCardData {
  id: string;
  title: string;
  category: string;
  imageUrl: string;
  screens: number;
}

export interface TestimonialData {
  id: string;
  name: string;
  role: string;
  company: string;
  image: string;
  text: string;
}

export interface LogoData {
  name: string;
  url: string;
}